# Deep ANC: Parallel Predictive Feedforward Active Noise Cancellation
# Core dependencies

# PyTorch (CUDA optional)
torch>=2.1.0
torchaudio>=2.1.0

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# Room acoustics simulation (optional, for advanced physics)
pyroomacoustics>=0.7.0

# Mamba SSM (State Space Model) - optional, fallback provided
# Requires CUDA toolkit (nvcc) to compile. Install separately if available:
#   pip install mamba-ssm
# mamba-ssm>=1.2.0

# Utilities
tqdm>=4.65.0
matplotlib>=3.7.0

# For data download
requests>=2.28.0

